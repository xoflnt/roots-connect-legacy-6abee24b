

# تحديث بيانات منيرة بنت زيد وعايد بن زيد

## التغييرات في `src/data/familyData.ts`

### 1. منيرة بنت زيد (id 500)
- تحديث `spouses` إلى 4 أزواج: `"محمد (الزوج الأول)، زوج ثانٍ (غير معروف)، عبدالعزيز القطنان، حمود العامر"`
- استبدال أبنائها بـ 7 أبناء (بدل 4):
  - **501** محمد بن محمد — من الزوج الأول محمد → `mother: "محمد (الزوج الأول)"`
  - **502** بشرى — من الزوج الثاني → `mother: "زوج ثانٍ (غير معروف)"`
  - **503** مقبل — من الزوج الثاني → `mother: "زوج ثانٍ (غير معروف)"`
  - **504** لولوه بنت عبدالعزيز → `mother: "عبدالعزيز القطنان"`
  - **505** مزنة بنت حمود → `mother: "حمود العامر"`
  - **506** سبيكة بنت حمود → `mother: "حمود العامر"`
  - **507** أحمد بن حمود → `mother: "حمود العامر"`

### 2. عايد بن زيد (id 600)
- تحديث `spouses` من `"نورة المهنا، عائلة النصار"` إلى `"نورة المهنا، بنت الناصر (الاسم غير معروف)"`

### 3. عبدالله بن عبدالعزيز (id 402)
- إضافة `birth_year: "١٣٥٥"` (كان فارغاً)

### ملاحظة تقنية
حقل `mother` يُستخدم كـ grouping key في `useTreeLayout.ts` لربط الأبناء بعقدة الزوج/الزوجة. لذلك يجب أن يتطابق مع اسم الزوج في حقل `spouses` — وهذا ما سيتم تطبيقه.

