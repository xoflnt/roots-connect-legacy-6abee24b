

# حذف العضوين نجلاء وعلي من قاعدة البيانات

## المشكلة
العضوان التاليان موجودان في قاعدة البيانات ويجب حذفهما:
- `USR-mmmu0mzy` — نجلاء (أنثى)
- `USR-mmmu0umg` — علي (ذكر)

كلاهما ليس لديه `father_id` وليسا في البيانات الثابتة.

## الخطوات

### 1. إضافة صلاحية حذف مؤقتة
جدول `family_members` لا يسمح بالحذف حالياً. سنضيف سياسة RLS للحذف عبر migration.

### 2. حذف السجلات
حذف العضوين من جدول `family_members` وأي سجلات مرتبطة في `verified_users` أو `family_requests`.

### 3. التحقق
لا يوجد أبناء مرتبطون بهما (لا أحد لديه `father_id` يشير إليهما).

