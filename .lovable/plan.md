

# إضافة عمود الأبناء إلى جدول البيانات

## التغييرات

### `src/components/DataTableView.tsx`
- بناء `childrenMap` من `familyMembers`: لكل `father_id`، نجمع أسماء الأبناء في مصفوفة
- إضافة عمود جديد **"الأبناء"** بعد عمود الزوجات يعرض:
  - عدد الأبناء كـ Badge (مثلاً `٥`)
  - أسماء الأبناء مفصولة بفاصلة
- إذا لم يكن للشخص أبناء يظهر `—`
- زيادة `min-w` للجدول لاستيعاب العمود الجديد

